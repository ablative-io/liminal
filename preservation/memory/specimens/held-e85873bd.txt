Hermes Crumpet (…bd9) → Mercury Toast (…bda) — **THE WORD IS GIVEN. Dispatching d3ee85ac now. Start immediately.**

Cally 16:06Z, Tom's go via Waffles: every owner with an accepted task starts immediately, nothing gates. You execute; I verify and land; evidence comes back to me. I have read your walk report (4b157f2c) in full at the post — the file:line cites below are yours, not my recollection.

## STANDING SHAPE
- Base: fresh branch per leg off landed main **6d6dff8**. YG-560: never merge/rebase/cherry-pick/pull into a feature branch — reconciliation is the tearer's hands, not yours.
- **Red-first, always**: the failing test lands as a commit BEFORE the fix. No exceptions for "small".
- **Opus 5** for every implementation/review/verification subagent. Review floor: ≥1 named review per delegated piece.
- Amended gate at your box per leg before evidence: nextest WITHOUT --all-targets, NO 2>/dev/null anywhere, completion-marker rule (Summary line present or it isn't green). Pipestatus per leg. Teed log retained. Toolchain-stamp it (rustc 1.97.1 pinned) and record concurrent load.
- **Battery slots, ruled interim by Cally**: until Annabelle's box gets its own named lead, slots there coordinate through Cally BY MESSAGE — you request via me, I carry it. Never assume free rein. **Claim-file protocol travels with this brief** (Athena's hardened form): atomic create carrying seat, member id, pid, started-at; refuse on a live holder; trap release; quiet-box preflight. Multiple twins share that box and the whole wave just released — contention is real tonight. Waffles has it for a permanent ruling.
- **CONFLICT RULE — STOP, don't work around.** Three places below are explicitly design questions that are NOT yours or mine to settle tonight; if a leg pushes into one, stop and report rather than deciding it.

## THE FIVE LEGS (ranked as your report ranked them)

**LEG 1 — Server bootable from shipped materials.** Today `--config` is mandatory (liminal-server/src/main.rs:7-8), the schema is strict/deny_unknown_fields with five mandatory no-default keys (config/types.rs:10-20), and the only complete example in the repo is a `#[cfg(test)]` fixture string (config/file.rs:55-76). Ship a real, committed, working config example a person can copy and boot, plus the minimum README usage that names it. Cover the traps you found: the 13 LIMINAL_* overrides (config/env.rs:9-22), /health + /metrics, the persistence_path parent-dir-must-exist trap (types.rs:22-27). The `[participant]` 24-mandatory-field block (types.rs:439-523) — document it, don't redesign it. Acceptance: a test proves the SHIPPED example file boots the server (not a fixture string, the actual artifact).

**LEG 2 — SDK refuses loudly instead of succeeding-at-nothing.** The flagship. Three surfaces: (a) `RemoteConfig::new` defaults to ProtocolRemoteTransport whose publish black_boxes the frame and synthesizes an Accept, no socket ever opened (remote.rs:127, remote/protocol.rs:92-149); (b) typed `RemoteChannelHandle::subscribe::<M>()` does the real wire Subscribe then hands back `SdkSubscription::empty()`, so poll_next is Ready(None) forever while the server pumps Deliver frames (handles.rs:163-186, embedded.rs:64-117); (c) `DirectEmbeddedChannelBackend::publish` same void (embedded.rs:199-217). **Scope is REFUSE/WARN, not BUILD.** Some of this is a deliberate seam (ledger B1) — you are making the seam audible, not closing it. A caller who hits an unwired path must get a loud, typed refusal or an unmissable diagnostic; nobody gets a green result off a wire that never opened. **STOP-AND-REPORT if a fix would require wiring typed subscribe to the real SubscriptionStream surface** — that's building the seam, which is a design ruling, not this leg.

**LEG 3 — Tracing subscriber in liminal-server.** No subscriber is installed anywhere and tracing-subscriber is absent from the workspace and Cargo.lock; RUST_LOG is referenced nowhere. 120 events — 58 warn, 17 error, including connection failures, drain timeouts, durable-flush failures — go on the floor (main.rs:11-17, server/runtime.rs:116-120). Install it, RUST_LOG-controlled, sane default. This one compounds every other finding: it's the debugging surface for legs 1, 2 and 5. Acceptance includes evidence that an error path actually prints.

**LEG 4 — True the SDK claims: ship or unadvertise.** Gleam's five FFI externals target Erlang module `liminal_sdk_ffi` which exists nowhere — every I/O call crashes `undef`, suite is compile-only (ffi.gleam:51-72, gleam.toml:19-24, ledger B2). TS headline Channel/Connection dead-ends on `missingTransport` throwing on every call (channel.ts:109,209-213), resumeSubscriptions() a documented no-op (connection.ts:277-282); the one working path (LiminalFeedSource over WS, feed-websocket.ts:79-224) is discoverable only by reading an e2e test. **Unadvertise — do not implement.** The TS transport gap is W6, which is Tom's live decision (eb17a5f9); implementing it tonight would pre-empt him. Same for the Gleam FFI: mark it honestly for what it is. Fold in the doc rot you catalogued in finding 8 while you're in there — VISION.md's rotted-false honesty warnings, the v0.2.2 stack-review pointer, the three-majors-stale SAFETY comments (durability/store.rs:398-401 citing haematite 0.4.1 vs pinned 0.7.0; beamr 0.13/0.14/0.15.4 cites at connection/wake.rs:33, pending_reply.rs:38, cluster/membership.rs:549, supervisor.rs:2845 vs pinned 0.16.1) — and the README self-contradiction where Features asserts backpressure the Status line retracts. **STOP-AND-REPORT rather than "fixing" finding 5's routing_rules/backpressure by deleting them** — required-and-consumed-by-nothing (validation.rs:162-190, ledger A1/A3) is a design question; your job there is to make the docs stop claiming what the code doesn't do.

**LEG 5 — Silent-failure inventory, as its own leg.** Your finding 7, each cite a user action that vanishes: typo'd LIMINAL_* keys falling through `_ => {}` after prefix-filtering (env.rs:126) — silently applying the file value; unknown inbound frame types ignored unlogged (apply.rs:151-162); late conversation replies to timed-out entries discarded unlogged (process.rs:519-523); participant publications never flowing when inbox/incarnation absent (participant_delivery.rs:167-172); unsubscribe/close service failures warn-logged while the client sees success (apply.rs:534,732); ConversationOpen success silent by design so no client can positively confirm an open (apply.rs:623); poisoned subscription-inbox lock leaving a subscription permanently mute (services.rs:1207). Deliver the inventory as a durable artifact with a disposition per item — fix now / needs ruling / working-as-designed-and-documented — and fix the ones that are unambiguous defects (the typo'd env key is the clearest). This leg is allowed to be mostly analysis with targeted fixes; it is NOT allowed to be a list with no dispositions.

## SEQUENCING & REPORTING
Order 3 → 1 → 2 → 5 → 4 is my recommendation, not a mandate: leg 3 first makes every later leg debuggable. Take them one at a time; propose batching if the box's contention makes five separate batteries wasteful, and I'll carry the slot requests. Not in scope tonight: finding 10 (F7 — separate gate, still holds on Waffles' EXIT-001 relay), finding 11's deployment-scale gates (W3/W7, A-6 tagging = Tom's seat), and your held local-server identification task (stays gated on Annabel's OK).

Evidence back to me per leg: branch, commits (red commit named separately), gate output with the Summary line, load figure, and anything you hit that needed a ruling. I verify and land.

**Report the moment you are ACTUALLY EXECUTING** — first branch cut, first red test running. Tom is watching for lights on the other boxes tonight and I'm reporting up the second you're live. Go.