---
name: emitter-design-round-2026-07-24
description: "HISTORICAL (superseded by waffles-batch-2026-07-28): emitter build DEFERRED BY RULING 07-28 (Waffles coordinator seat; wake trigger named in doc r5; branch deleted @ deeed3e; Tom has NOT ruled the A′ substrate question); design r4 + Detached drain torn+landed; handshake lane landed+released 0.5.0"
metadata: 
  node_type: memory
  type: project
  originSessionId: 6bfc46aa-2bfa-43f8-a284-0ba8a3713539
  modified: 2026-07-28T13:33:41.899Z
---

# Emitter design round (dispatched 2026-07-23 ~15:26Z via Waffles; worked 2026-07-24 AEST). Related: [[frame-path-lanes-2026-07-23]], [[ceremony-reduction-directive]], [[liminal-repo-state]]

## ⚡ STATE AT REST — one trigger reactivates my seat
**Liminal main = `6d09bae`** (design r4 + Detached drain-extension both landed; **the estate's one live known defect, LIM-DETACHED-PENDING, is CLOSED**). My checkout ff'd; detached-drain worktree + local branch swept (origin `fix/pending-detached-drain` sweepable whenever).

**⚠️ SUPERSEDED 2026-07-28 — the hold is DISSOLVED, see [[waffles-batch-2026-07-28]].** The S-5 fork was RULED by Waffles at the coordinator seat (Option 1 = ratified build shape; Option 2 rejected; BUILD DEFERRED on Finding A′; connection-host pids permanently out of S-6; named wake trigger = first production participant-correlated beamr spawn). Tom has NOT ruled the underlying substrate question — it stays open at his desk; the deferral is answer-neutral. The ruling + survey were folded into PENDING-DRAIN-EMITTER **r5** and landed on main (3bae8ba); `feat/process-killed-emitter` was DELETED (head was deeed3e) and both worktrees swept. Nothing at this seat waits on the emitter anymore; the wake trigger lives in the doc (r5 §4.6/S-20), not in a held lane. The paragraphs below are the historical record of the hold as it stood.

### The fork + rulings already in hand (don't re-derive)
- **The fork (spot-verified at my hands at 6d09bae):** the W4 reclaim reactor claims beamr's ONE `subscribe_exit_events` at supervisor construction (`supervisor.rs:1055` → `liminal-connection-reclaim` thread `:1063-1067`) and is SOLE DRAINER — `deliver_reclamation` does `drop(take_exit_outcome(pid))`, discarding the pair S-15 must consume (`:2661-2670`); its Lagged path PEEKS non-consuming (`:2849`) vs S-7's take. S-5 "adapter claims it" unimplementable as written (set-once sender).
- **S-5 RULED by Waffles (conditional on build proceeding): Option 1 — adapter = EXTENSION of the reclaim reactor.** One component, one subscription, S-15 total filter inside it, take-vs-peek unified on S-7's take. Options 2 (builds the excluded multiplex) and 3 (no substrate) DEAD. Amendment LANDED dated in the design doc at my hands (e443c8b → deeed3e, pushed; Waffles' hold re-pinned to deeed3e).
- **Finding A′ (the Tom question):** NO production component spawns a participant-correlatable beamr process at 6d09bae — the only production scheduler is the connection scheduler (processes = connection hosts, already ConnectionLost-classed on death; mapping them double-fates); liminal-crate schedulers host channel/conversation actors only. S-12's "actor-granular death in a live VM" premise has no substrate in this tree.
- **Banked clean for the resumed build:** §8.7 re-corroborated at the CONSUMED pin — beamr **0.16.1** registry bytes (all §4.2 facts hold at the design's cited lines); WAKE OBLIGATION recorded in the survey doc: check whether the pin advances to 0.16.2 (memory-safety patch queue-jumped) before building. §8.8 formal census = no production mint of ProcessKilled (fenced-attach inbound arm echo-only + refuses mismatch; wire-push decode = client-side leg; crash repo cfg(test); trybuild wall intact `tests_w1b_connection_fate.rs:419-423`). Early-warning pass had also confirmed all beamr facts at 0.16.2 @ 293d368 (store logic moved to `exit_tombstones.rs`, semantics identical).

## Landed record of the round (all torn by Waffles, all on main)
1. **Design r4** (`docs/design/PENDING-DRAIN-EMITTER.md`, landed at c41af22, tearer-merged): pin rebase to 0.4.1 tree; §3.0 landed-drain record; **S-14 SUPERSEDED-BY-REALITY** (drain forced protocol 0.3.2 via public lifecycle API `drain_pending_terminal` with zero wire change — lesson: protocol-crate API ≠ wire schema, BOTH bump; emitter needs NO new wire vocab, `DiedCause::ProcessKilled` exists end-to-end → production source only); **§3A S-16/S-17/S-18 = the LIM-DETACHED-PENDING ruling** (faithful detach finalization, never erasure — R-A2 "release" reading RATIFIED at tear as release-from-pending-residence); **§8A S-19 = ASK-6 sequenced OUT** with reasoning + non-interference commitment (ASK-6 text of record: frame `origin/feat/f3b-pubsub-workflow` @ 4767f14 `docs/upstream/liminal-asks-2026-07-23.md` — the 07-20 ledger tops at ASK-4; ASK-6 board row CREATED on Tom's tracker).
2. **Detached drain-extension** (branch landed at 6d09bae, tear ~18:02Z PASS): server-only, ZERO protocol changes; S-17 spelling = new `StoredDetachedSource::Drained{StoredDrainedTerminal}` source tag (additive, replay-by-shape, cause-preserving); red pin 44686d8 red-first (log folded forward at 6d09bae per review minor-1 — reviewed head 98dcf78 + evidence-only commit, re-declared, accepted); nine S-18 pins (five in `tests_restore_window_detached.rs`, mixed-flavor `:818` sequential+trip-wired, `e2e_terminal_drain_detached.rs:284` with epoch rotation beyond contract); battery GREEN 1785/0/42 (+6 exact) @ 4a5de6a; named Fable review PASS-WITH-FINDINGS no MAJOR. All 5 declared deviations accepted (CleanDeregister pin-9 mint sized correct — ServerShutdown fates every socket). **T1 RULED: projection silence ACCEPTED** (server-only charter bound; new wire-record class = protocol surface = out of bounds by construction) — question stays open as a NAMED BOARD ROW: follow-up projection lane, next design round ("peer can see re-Attach without ever seeing victim leave Bound" recorded verbatim; Leave differs because it mints a Left record — conversation-surface act vs binding-lifecycle act). Reviewer notes (invariant-path slot hardening; stale occurrence parity) recorded, no action.

## Durable facts (verified this round; still true at 6d09bae unless noted)
- Old Detached tear gate was `ops_terminal_drain.rs:325-329` — now REMOVED by the landed fix; protocol drain ops were already flavor-agnostic; candidate selection flavor-blind (claim_frontier.rs:553-562).
- Pending-Detached minters: `start_blocked_detach` (detach.rs:511-545) + ServerShutdown (contract :603); `complete_pending_detach` (detach.rs:553-577) = the faithful completion the fix uses for blocked-explicit; shutdown-minted commits member terminal directly.
- `AttemptTokenBodyConflict` = CredentialAttach + Leave arms only (response.rs:59-86) — ASK-6's gap. Liminal pre-ack = client.rs rule 15 (:42-46) + rule 14.
- Estate context: beamr now 0.16.2 published (liminal consumes 0.16.1 at 6d09bae); frame F-3b acts, F-6a, WPORT-3, beamr 0.16.2 landed during the 07-23 hold (per RUNBOOK ratification note).
- Scout-conflict lesson: one scout claimed contract R-D1 pins drifted, another said SAME — I verified SAME at my own hands. Resolve scout conflicts personally before binding them into a doc.

## Standing / ops lessons (this round)
- **gates RUNBOOK = `/Users/annabel/Developer/ablative/stack/gates/RUNBOOK.md`** (fresh clone of github.com/ablative-io/gates; old `ablative/gates` folder is a STALE NON-REPO copy). Doctrine: attestation, red-first (commit the red LOG, not just the message — the one review finding this round), tears-pin-to-heads (moved head ⇒ re-declare), stop-and-ask at contract forks (paid off twice: ASK-6 premise check, S-5 fork), amendments-land-in-the-artifact (I landed Waffles' S-5 ruling in-doc same hour), evidence-wins + supersession-recorded-where-ruling-lives (S-14).
- Watchdogs: arm the UNION of transcript + teed-log mtimes from the start (transcript-only false-trips during long compile loops); stop at fold (completed outputs false-trip). Worker death on usage limit ⇒ salvage check first (branch existed at base only ⇒ clean redispatch reusing scaffolding). Stranded-waiter (worker yields on running battery) ⇒ ground-truth pid, pid-wait, resume-with-verdict — worked cleanly.
- Norn automation (aion-runner yggdrasil worktrees) runs concurrent cargo on this box — separate target dirs, harmless, but battery reports record it; apply the suite-count tell.
- Waffles miscounted §3A.7 pins (six vs nine) from a windowed read — quote-the-doc-not-the-count; truing accepted immediately.

## LIMINAL-HANDSHAKE-PROTOCOL lane (updated 2026-07-28)
Second open item at my seat (announced by Waffles 07-27 into my dead session; Vesper check-in 07-28, Tom-prompted). The liminal half of aion's ratified worker-handshake work: ONE frame change — WorkerRegister gains a trailing activity census (u32 count + per-descriptor name/input_schema_json/output_schema_json). Delta preserved on `origin/rescue/handshake-protocol-snapshot` @ 8c6679bb (8 files +95/−19, "NOT FOR REVIEW" snapshot) — VERIFIED AT MY HANDS 07-28. Vesper's seed brief RECOVERED 07-28 (routing loss — original went to Cally's thread, not my dead session): full verbatim text banked in [[handshake-lane-seed-brief-2026-07-27]]. Key facts from her summary (verify against the re-sent brief before use): old→new safe via is_finished() sniff (empty census = pre-contract worker); new→old breaks loudly ⇒ servers-before-workers deployment, 0.5.0-shaped bump; TRAP: the subscription.rs hunk is vendoring collateral that REVERSES beamr 0.16.1's spawn_native_trap_exit race fix — DROP it (Artemis owns that ruling). aion side build/worker-handshake @ 70c07ef8 pushed/reviewed/ratified, waits on my lane landing to drop its vendored patch and re-gate. **LANE AUTHORIZED AND STARTED 2026-07-28 ~04:45Z**: Tom's start-now word arrived via VESPER (not the usual Waffles relay — routing anomaly); I HELD per discipline and put the call to ANNABEL IN-SESSION, who ruled "Start it now" (operator authorization on the record). Scope = the banked brief exactly; **NO PUBLISH — Tom cuts the release from his box** ("we'll need to do it from here"); when pushed+landed-per-normal-gates, REPORT TO VESPER (she relays to Tom; her aion re-pin follows the publish). Queue: this lane takes front slot per Tom-via-Vesper; emitter hold unchanged behind it; Vesper offered to resolve any Waffles conflict if one surfaces. SURVEY DONE AT MY HANDS: subscription.rs hunk byte-confirmed as Vesper described (deletes the 0.16.1 spawn_native_trap_exit pre-runnable-by-construction call+comment, replaces with spawn_native + host-side set_trap_exit — the exact post-spawn race window the 0.16.1 API closed) ⇒ DROP; take only the 7 protocol files (codec.rs, codec/known.rs, codec/payload.rs, codec/tests.rs, codec/tests_support.rs, frame.rs, mod.rs). **BUILD COMPLETE AT MY SEAT 2026-07-28 (~05:10Z)**: branch `feat/handshake-protocol` (worktree `.worktrees/handshake-protocol`) = 6d09bae → **8eda999** (7-file rescue extraction, subscription.rs dropped with reasoning in commit msg) → **4c2a4d8** (pins leg, Sol session 09e8c36a, envelope ~/.norn/delegations/claude-dev-handshake-pins.yXgR6V), pushed, clean, 0/0 vs origin. SEAT RULING MADE: sniff KEPT (codec layer version-blind both paths; old payload ends exactly at identity so is_finished() is precise; version-gating = structural change beyond ratified delta) — costs recorded as PIN A + ceiling comment in known.rs ("sniff consumes WorkerRegister's ONE trailing-bytes extension slot; future fields ride a ProtocolVersion gate"). Pins: `worker_register_old_shape_decodes_to_empty_census` (encode-empty-census-then-truncate-proven-zero-count construction, production write_header rewrite, byte-exact payload assert, + new-empty-census convergence edge) and `worker_register_nonempty_census_round_trips_exactly` (2 descriptors incl. empty-string schema, encoded_len match). Battery GREEN at 4c2a4d8: 1789/0/42 — suite-count tell ACCOUNTED EXACTLY: +2 liminal-rs lib (336→338) AND +2 liminal-ts-wasm lib (98→100) because ts-wasm compiles the protocol module via `#[path]` include (sdks/liminal-ts/wasm-src/src/lib.rs:1) — the pins count twice. ⚠️ lesson re-confirmed: shared CARGO_TARGET_DIR served a stale cross-worktree ts-wasm binary during my fold (main briefly "showed" 100) — committed 98dcf78 gate-log was the true baseline. Leg deviations, both ACCEPTED at named Fable review (Hermes seat, at the bytes, PASS): test-file split codec/tests/worker_register.rs (5 pre-existing tests moved VERBATIM — compared) + three server fixture one-liners `activities: Vec::new()` which were REQUIRED (my commit-1 gating was `-p liminal-rs` only; 8eda999 had broken workspace all-targets — gating gap at my hands, disclosed). NO manifest/version edits (0.5.0 rides Tom's release side). **LANDED 2026-07-28 ~05:20Z, VERIFIED AT MY HANDS**: tear ran at VESPER's seat (Tom moved it from Waffles); **liminal main = `62f3f80`** — clean fast-forward 6d09bae → 8eda999 → 4c2a4d8 → 62f3f80 (her evidence commit: fresh-worktree battery at merged tree, fmt 0/clippy 0/1789-0-42, +4 accounting held, both pins observed both legs, drop-reasoning + ceiling comment confirmed). I verified via fetch: origin/main = 62f3f80, 4c2a4d8 is ancestor. My main checkout ff'd to 62f3f80. NO publish/tag/manifest edits — release cuts from TOM'S BOX next, then Vesper's aion re-pin. LANE CLOSED AT MY SEAT (task #21 completed). Housekeeping owed next session: sweep `.worktrees/handshake-protocol` + local branch (NOT swept in-session — it is the session's primary working dir); origin `feat/handshake-protocol` sweepable whenever.
