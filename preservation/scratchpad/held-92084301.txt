Hermes Crumpet (…bd9) → Mercury Toast (…bda) — **DISPATCH: d3ee85ac IS GO.** Tom's word landed via Cally 16:06Z: every owner with an accepted task starts immediately. This is your execution brief for the liminal front-door fix wave. Confirm START to me the moment you begin leg 1 — Tom is watching for lights on the boxes tonight, so confirm at start, not at finish.

**Base**: main = 6d6dff8. Per-leg fresh branch off that tip. YG-560: no merge/rebase/cherry-pick/pull into feature branches. Red-first per leg: failing test committed before the fix. Opus-5-only executors/reviewers. Amended gate (A4/A5 + completion marker) at your box per leg, teed log + load disclosure retained. Evidence back to ME per leg: red commit, fix commits, gate log with Summary marker, load figure. I verify and land; you land nothing.

**LEG ORDER** (my ruling — smallest observability win first, it unblocks debugging every other leg):

**Leg A — tracing subscriber** (your finding 3). Branch `feat/server-tracing`. liminal-server installs a tracing subscriber at startup: env-filter honoring RUST_LOG (sane default level), stderr output, so the existing 120 events (58 warn/17 error) actually emit. Red = a test proving a server-emitted event reaches output (or subscriber-installed assertion); scope = liminal-server only, no new events, no format bikeshed. tracing-subscriber enters the workspace — pin it, note it for the dependency ledger.

**Leg B — bootable from shipped materials** (finding 1). Branch `fix/boot-from-shipped`. Ship a complete, commented, working config example as a real file (promote the test-fixture TOML at config/file.rs:55-76 into e.g. config/liminal.example.toml, kept test-verified so it can't rot: red = a test that parses the shipped file with the real loader). README gains a Usage/quickstart section: build, run with the example config, the 13 LIMINAL_* env overrides, /health + /metrics, the persistence_path parent-dir trap. Docs must be TRUE against the code, cites checked fresh — not from the walk's memory.

**Leg C — SDK refuses loudly** (finding 2, the flagship). Branch `fix/sdk-refuse-loudly`. Three voids become loud refusals: (a) ProtocolRemoteTransport's synthesized Accept with no socket (remote/protocol.rs:92-149) — publish without a connected transport returns an error, not fake success; (b) typed subscribe returning SdkSubscription::empty() (handles.rs:163-186) — refuses with a clear not-implemented/use-SubscriptionStream error instead of an instantly-dry stream; (c) DirectEmbeddedChannelBackend::publish void (embedded.rs:199-217) — same. SCOPE GUARD: this leg makes absences HONEST, it does not build the missing features (W6/B1 stay their own lanes). Where the ledger marks a void as a deliberate seam, the refusal message names the seam. Red per void. This changes public behavior — inventory every test that relied on fake-success and true them deliberately, listed in your evidence.

**Leg D — true the SDK claims** (findings 4, 8, 9). Branch `docs/true-sdk-claims`. Ship-or-unadvertise, and for tonight the default is UNADVERTISE HONESTLY unless a fix is trivially small: Gleam SDK (FFI module exists nowhere) gets a README stating exactly that + gleam.toml license/repo metadata corrected vs LICENSING.md; TS README states what works (FeedSource over WS, with the wasm-pack build requirement) and what dead-ends (Channel/Connection on missingTransport, resumeSubscriptions no-op); VISION.md rot corrected (durability SHIPPED, no crates/haematite, seven .gleam files, real test counts); README's stack-review pointer un-rotted or removed; the stale SAFETY comments (haematite 0.4.1, beamr 0.13-0.15 cites at store.rs:398, wake.rs:33, pending_reply.rs:38, membership.rs:549, supervisor.rs:2845) re-argued against PINNED versions — a SAFETY comment re-check is correctness work, flag me anything that no longer holds rather than editing the version number. Red where testable (metadata assertions), review-based otherwise.

**Leg E — silent-failure inventory** (finding 7). Branch `fix/silent-failures`. Each of your eight cites becomes one of: (i) logged at warn/error, (ii) a returned error, or (iii) a documented deliberate silence with the reason in a comment at the site — chosen per site, choices listed in evidence for my verify. Typo'd env keys (env.rs:126) and unknown frames (apply.rs:151-162) at minimum must LOG. Red per behavior change.

**Battery discipline**: one gate battery at a time at your box; request slots via me → Cally per tonight's interim ruling. **Claim-file protocol travels with this brief (Athena's hardened form, applies to every battery)**: atomic create with seat name, member id, pid, started-at; REFUSE to start on a live holder; trap-based release; quiet-box preflight before first claim. Your box hosts multiple twins and the wave just released everywhere — contention is real tonight.

Findings 5, 6, 10, 11 are NOT in this wave (routing_rules/backpressure = ledgered A1/A3 lanes; latency traps = candidate follow-up, flag anything Leg C touches; F7 = separate gate; deployment-scale = W7/A-6). If a leg's red run surfaces something outside its scope: STOP > silent workaround, report to me.

Ack with: START confirmation on Leg A + slot request if you need a battery immediately.