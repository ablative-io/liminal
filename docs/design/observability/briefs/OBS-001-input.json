{
  "repo_root": "/Users/tom/Developer/ablative/liminal",
  "brief_id": "OBS-001",
  "reviewers": [
    "Waffles the Terrible"
  ],
  "base_ref": "main",
  "placement": "local",
  "isolation": "worktree",
  "brief_document": {
    "id": "OBS-001",
    "cluster": "observability",
    "title": "Establish metrics registry infrastructure with atomic snapshot support",
    "depends_on": [],
    "blocked_by": [],
    "checklist": [
      "C1",
      "C2"
    ],
    "stories": [
      "S5"
    ],
    "design_anchor": [
      "ADR-004"
    ],
    "purpose": "Bootstrap the metrics subsystem with the module skeleton and the MetricsRegistry type that all metric collectors register into. The registry must support atomic snapshot reads so that Prometheus scrapes see a consistent view across all metric families. This is the leaf dependency for every other metrics brief (OBS-002 through OBS-005).",
    "task": "Create the metrics module at crates/liminal/src/metrics/ with a mod.rs that contains only pub mod and pub use declarations. Implement MetricsRegistry in registry.rs as the central coordination point for metric collectors. The registry must support registration of counters, gauges, and histograms by name, and provide an atomic snapshot method that returns a consistent read of all registered metrics. Add pub mod metrics to lib.rs. The registry must be Send + Sync, Clone (handle-style), and impose zero overhead when no exporter is attached.",
    "requirements": [
      {
        "id": "R1",
        "title": "Create metrics module skeleton",
        "spec": "crates/liminal/src/metrics/mod.rs SHALL declare pub mod for registry (and future sub-modules channel, conversation, pressure, export). mod.rs SHALL contain only pub mod declarations and pub use re-exports. mod.rs SHALL NOT contain any function, struct, trait, or impl definitions.",
        "acceptance": [
          "crates/liminal/src/metrics/mod.rs exists",
          "mod.rs declares pub mod registry",
          "mod.rs contains zero function definitions, zero struct definitions, zero trait definitions",
          "cargo check -p liminal passes"
        ],
        "files": {
          "create": [
            "crates/liminal/src/metrics/mod.rs"
          ],
          "modify": [],
          "delete": []
        },
        "checklist": [
          "C1"
        ],
        "stories": []
      },
      {
        "id": "R2",
        "title": "Add pub mod metrics to lib.rs",
        "spec": "crates/liminal/src/lib.rs SHALL declare pub mod metrics so the metrics subsystem is part of the public API surface. lib.rs SHALL re-export MetricsRegistry from the metrics module.",
        "acceptance": [
          "lib.rs contains pub mod metrics declaration",
          "MetricsRegistry is re-exported from lib.rs",
          "cargo check -p liminal passes"
        ],
        "files": {
          "create": [],
          "modify": [
            "crates/liminal/src/lib.rs"
          ],
          "delete": []
        },
        "checklist": [
          "C1"
        ],
        "stories": []
      },
      {
        "id": "R3",
        "title": "Define MetricKind enum for counter, gauge, and histogram",
        "spec": "MetricKind SHALL have exactly three variants: Counter, Gauge, and Histogram. Counter is a monotonically increasing u64. Gauge is a signed i64 that can go up or down. Histogram records individual observations into configurable buckets. MetricKind SHALL derive Debug, Clone.",
        "acceptance": [
          "MetricKind::Counter, MetricKind::Gauge, and MetricKind::Histogram are the only variants",
          "MetricKind derives Debug and Clone",
          "MetricKind is defined in crates/liminal/src/metrics/registry.rs"
        ],
        "files": {
          "create": [
            "crates/liminal/src/metrics/registry.rs"
          ],
          "modify": [],
          "delete": []
        },
        "checklist": [
          "C2"
        ],
        "stories": []
      },
      {
        "id": "R4",
        "title": "Define MetricValue types with atomic backing",
        "spec": "Counter SHALL be backed by AtomicU64. Gauge SHALL be backed by AtomicI64. Histogram SHALL store observations in a thread-safe structure with configurable bucket boundaries. All metric values SHALL be readable without locking the writer path. Recording a metric value SHALL NOT block the caller \u2014 all updates are atomic or lock-free.",
        "acceptance": [
          "Counter uses AtomicU64 with Relaxed ordering for increments",
          "Gauge uses AtomicI64 with Relaxed ordering for set/increment/decrement",
          "Histogram stores observations in a thread-safe bucketed structure",
          "No Mutex or RwLock is held during metric recording"
        ],
        "files": {
          "create": [],
          "modify": [
            "crates/liminal/src/metrics/registry.rs"
          ],
          "delete": []
        },
        "checklist": [
          "C2"
        ],
        "stories": []
      },
      {
        "id": "R5",
        "title": "Implement MetricsRegistry with registration and lookup",
        "spec": "MetricsRegistry SHALL allow registering named metrics with labels. Registration SHALL return a handle to the metric for recording. MetricsRegistry SHALL be Clone (Arc-wrapped inner), Send + Sync. Duplicate registration of the same name with incompatible MetricKind SHALL return an error. MetricsRegistry SHALL NOT impose any limit on the number of registered metrics.",
        "acceptance": [
          "MetricsRegistry::new() creates an empty registry",
          "register_counter(name, labels) returns a CounterHandle",
          "register_gauge(name, labels) returns a GaugeHandle",
          "register_histogram(name, labels, buckets) returns a HistogramHandle",
          "MetricsRegistry is Clone + Send + Sync",
          "Registering the same name with a different MetricKind returns Err"
        ],
        "files": {
          "create": [],
          "modify": [
            "crates/liminal/src/metrics/registry.rs"
          ],
          "delete": []
        },
        "checklist": [
          "C2"
        ],
        "stories": [
          "S5"
        ]
      },
      {
        "id": "R6",
        "title": "Implement atomic snapshot reads",
        "spec": "MetricsRegistry::snapshot() SHALL return a MetricsSnapshot containing a consistent point-in-time view of all registered metrics and their current values. The snapshot SHALL read all metrics atomically so that no partial update is visible. MetricsSnapshot SHALL implement Debug. The snapshot SHALL NOT hold references to the live registry \u2014 it is a fully owned copy.",
        "acceptance": [
          "MetricsRegistry::snapshot() returns MetricsSnapshot",
          "MetricsSnapshot contains name, labels, kind, and value for every registered metric",
          "MetricsSnapshot implements Debug",
          "MetricsSnapshot is fully owned (no lifetime parameters)",
          "Concurrent writes during snapshot do not cause data races or panics"
        ],
        "files": {
          "create": [],
          "modify": [
            "crates/liminal/src/metrics/registry.rs"
          ],
          "delete": []
        },
        "checklist": [
          "C2"
        ],
        "stories": [
          "S5"
        ]
      },
      {
        "id": "R7",
        "title": "Zero overhead when no exporter is attached",
        "spec": "When no MetricsRegistry is installed, all metric recording operations SHALL compile to no-ops or return immediately without performing any work. The system SHALL support a global optional registry that metric sites check before recording. The check SHALL be a single atomic load, not a lock acquisition.",
        "acceptance": [
          "A global Option<MetricsRegistry> or AtomicPtr is provided for metric sites",
          "When the global registry is None, recording is a no-op (returns immediately)",
          "The check is a single atomic load, not a Mutex::lock",
          "No allocation occurs when metrics are disabled"
        ],
        "files": {
          "create": [],
          "modify": [
            "crates/liminal/src/metrics/registry.rs"
          ],
          "delete": []
        },
        "checklist": [
          "C2"
        ],
        "stories": []
      },
      {
        "id": "R8",
        "title": "Add metrics dependencies to Cargo.toml",
        "spec": "crates/liminal/Cargo.toml SHALL NOT add any external metrics library (no prometheus crate, no metrics crate). The metrics types are hand-rolled using std::sync::atomic. Cargo.toml SHALL NOT add any new dependencies for this brief \u2014 std atomics are sufficient.",
        "acceptance": [
          "No new dependencies added to Cargo.toml for metrics primitives",
          "AtomicU64 and AtomicI64 from std::sync::atomic are used directly",
          "cargo check -p liminal passes"
        ],
        "files": {
          "create": [],
          "modify": [
            "crates/liminal/Cargo.toml"
          ],
          "delete": []
        },
        "checklist": [
          "C1"
        ],
        "stories": []
      }
    ],
    "boundaries": [
      "SHALL NOT implement channel-specific metrics \u2014 that is OBS-002.",
      "SHALL NOT implement conversation-specific metrics \u2014 that is OBS-003.",
      "SHALL NOT implement pressure-specific metrics \u2014 that is OBS-004.",
      "SHALL NOT implement Prometheus text rendering \u2014 that is OBS-005.",
      "SHALL NOT add any external metrics or telemetry crate dependency.",
      "SHALL NOT implement trace context or structured logging \u2014 those are OBS-006 and OBS-007."
    ],
    "verification": [
      "cargo check -p liminal passes clean.",
      "cargo clippy -p liminal -- -D warnings passes clean.",
      "cargo fmt -p liminal --check passes clean.",
      "metrics/mod.rs contains only pub mod and pub use \u2014 no logic.",
      "MetricsRegistry is Clone + Send + Sync.",
      "Metric recording does not acquire any Mutex or RwLock.",
      "MetricsSnapshot is fully owned with no lifetime parameters."
    ]
  },
  "resolved_context": {
    "adrs": [
      {
        "id": "ADR-004",
        "title": "Backpressure as a wire protocol primitive with accept/defer/reject signals",
        "decision": "Backpressure is part of the wire protocol with three signals: accept (delivered), defer (buffered, will deliver when capacity frees), reject (consumer overwhelmed, message shed). Consumers declare capacity. The bus mediates between producer rate and consumer capacity. The rejected alternative is Kafka-style consumer-lag-only backpressure (not actionable by producers in real time).",
        "quote": "",
        "decided_by": "Bono + Frodo alignment"
      }
    ],
    "checklist": [
      {
        "id": "C1",
        "text": "Metrics module at crates/liminal/src/metrics/mod.rs contains only pub mod and pub use declarations."
      },
      {
        "id": "C2",
        "text": "MetricsRegistry defined in crates/liminal/src/metrics/registry.rs holds all metric collectors and supports atomic snapshot reads."
      }
    ],
    "stories": [
      {
        "id": "S5",
        "text": "As an Operator, I want to scrape metrics via a Prometheus-compatible endpoint so that I can integrate with my existing monitoring infrastructure."
      }
    ],
    "constraints": [
      {
        "id": "CN1",
        "text": "No file over 500 lines. Extract into submodules if approaching the limit."
      },
      {
        "id": "CN2",
        "text": "No panics in production code. .unwrap() and .expect() only in tests."
      },
      {
        "id": "CN3",
        "text": "mod.rs contains ONLY pub mod declarations and pub use re-exports. No logic."
      },
      {
        "id": "CN4",
        "text": "All public types implement Debug. All error types implement std::error::Error."
      },
      {
        "id": "CN5",
        "text": "Metrics instrumentation points in channel and conversation actors must not block the actor's message processing. Recording a metric is a non-blocking operation."
      },
      {
        "id": "CN6",
        "text": "Trace context propagation is transparent to application code. No manual context passing required for intra-bus messages."
      },
      {
        "id": "CN7",
        "text": "Prometheus export renders a consistent snapshot. Metrics do not change mid-render -- the registry provides an atomic read."
      },
      {
        "id": "CN8",
        "text": "Structured log entries never contain free-form string messages. Every field is a typed key-value pair."
      }
    ],
    "intention": "When this cluster is complete, an operator can see inside the running bus: which channels are hot, which consumers are under pressure, which conversations are stalling, and how messages flow across a multi-step exchange. The observability layer does not change how the bus works -- it makes the bus's existing behaviour visible.\n\nThe goal is operational confidence. An operator should be able to capacity-plan from metrics, diagnose delivery issues from traces, and correlate structured logs across conversation boundaries. An application developer should be able to trace a message through a conversation chain without instrumenting their own code.",
    "design_path": "docs/design/observability",
    "provenance": {
      "requested_by": "Tom",
      "quote": "When this cluster is complete, an operator can see inside the running bus: which channels are hot, which consumers are under pressure, which conversations are stalling, and how messages flow across a "
    }
  },
  "verify_fix_cap": 3,
  "review_cap": 1,
  "round_backoff_ms": 2000,
  "review_deadline_ms": 300000,
  "workspace_id": "2d5fdd51-1f25-45a4-8f86-4d4c978d1355",
  "clone_url": "https://github.com/ablative-io/liminal.git"
}
