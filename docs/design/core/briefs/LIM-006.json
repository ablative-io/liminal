{
  "id": "LIM-006",
  "cluster": "core",
  "title": "Implement causal context and per-chain ordering primitives",
  "depends_on": ["LIM-003"],
  "blocked_by": [],
  "checklist": ["C18"],
  "stories": [],
  "design_anchor": ["ADR-005"],
  "purpose": "Implement the causal ordering primitives that enable per-causal-chain message ordering without requiring global ordering (ADR-005). Messages carry a causal context with an optional parent reference. The ordering enforcement is a channel-level concern — this brief provides the data model and the ordering check functions that the channel actor and routing engine use.",
  "task": "Flesh out the CausalContext type from LIM-003 R5 with ordering utilities. Implement: MessageId generation (UUID-based), happened-before relationship detection from parent chains, and a CausalOrderer that buffers and delivers messages in causal order (children after parents) while allowing independent messages to flow in parallel. The orderer is used by the channel actor's delivery path when causal ordering is enabled on a channel.",
  "requirements": [
    {
      "id": "R1",
      "title": "Implement MessageId and causal chain tracking",
      "spec": "MessageId SHALL be a UUID-based unique identifier assigned to each message on publish. CausalContext SHALL track the full parent chain as a linked list of MessageIds (parent → grandparent → ...). A helper SHALL determine whether message A happened-before message B by checking if B's chain includes A's MessageId.",
      "acceptance": [
        "MessageId is a newtype over Uuid",
        "MessageId::new() generates a unique ID",
        "CausalContext::happened_before(ancestor_id) returns true if ancestor_id is in the parent chain",
        "Test: A → B → C chain; C.happened_before(A) == true; A.happened_before(C) == false; independent D.happened_before(A) == false"
      ],
      "files": {
        "create": [],
        "modify": ["crates/liminal/src/causal.rs"],
        "delete": []
      },
      "checklist": ["C18"],
      "stories": []
    },
    {
      "id": "R2",
      "title": "Implement CausalOrderer for buffered in-order delivery",
      "spec": "CausalOrderer SHALL accept incoming messages and emit them in causal order: a child message is held until its parent has been emitted. Independent messages (no parent or parent already emitted) are emitted immediately. The orderer SHALL detect orphaned messages (parent never arrives) via a configurable timeout and emit them with a warning.",
      "acceptance": [
        "CausalOrderer::submit(envelope) either emits immediately (independent or parent already seen) or buffers (parent pending)",
        "Buffered messages are emitted when their parent is submitted and emitted",
        "Independent messages pass through without buffering",
        "Orphan timeout: messages whose parent hasn't arrived within the timeout are emitted with a warning flag",
        "Test: submit B (parent=A), submit A → emits A then B; submit C (no parent) → emits immediately"
      ],
      "files": {
        "create": ["crates/liminal/src/causal/orderer.rs"],
        "modify": ["crates/liminal/src/causal.rs"],
        "delete": []
      },
      "checklist": [],
      "stories": []
    }
  ],
  "boundaries": [
    "SHALL NOT implement global ordering — causal ordering only.",
    "SHALL NOT implement the routing engine's use of causal ordering — that is the routing cluster.",
    "SHALL NOT add persistence of causal chains — that is the durability cluster."
  ],
  "verification": [
    "cargo test -p liminal passes clean.",
    "cargo clippy -p liminal -- -D warnings passes clean.",
    "CausalOrderer delivers parent before child, independent messages in parallel.",
    "happened_before correctly traverses the parent chain."
  ]
}
