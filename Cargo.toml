[workspace]
members = [
    "crates/liminal",
    "crates/liminal-protocol",
    "crates/liminal-sdk",
    "crates/liminal-server",
    "sdks/liminal-ts/wasm-src",
]
resolver = "2"

[workspace.package]
edition = "2024"
rust-version = "1.85"
license = "AGPL-3.0-only"
repository = "https://github.com/ablative-io/liminal"
authors = [
    "Tom Whiting <tom@ablative.com.au>",
]

# Single source of truth for every dependency version in the workspace. Members
# inherit with `{ workspace = true }` and layer on per-crate features. The two
# internal crates carry both a path (for local builds) and a version (what
# `cargo publish` ships to crates.io). `liminal` is published as `liminal-rs`
# because the bare name is taken on crates.io, but is imported as `liminal`.
[workspace.dependencies]
liminal = { package = "liminal-rs", version = "0.6.1", path = "crates/liminal" }
liminal-protocol = { version = "0.7.1", path = "crates/liminal-protocol" }
# PATH-ONLY, deliberately: the sole consumer is `liminal-server`'s
# dev-dependency (the loopback pins need the SDK client with `embedded` on),
# and a version here would flow into server's GENERATED manifest at publish
# time — where it deadlocks the wave, because the requirement now carries the
# `embedded` feature that no already-published sdk has, while the sdk's own
# `embedded` needs a published server. A version-less dev-dependency is
# STRIPPED from the published manifest, which is the standard dissolution of
# a dev-dep cycle; consumers never see dev-deps either way.
liminal-sdk = { path = "crates/liminal-sdk" }
# Consumed by `liminal-sdk`'s optional `embedded` feature (the in-process
# transport needs the server it connects to) and dev-consumed the other way, so
# the pair is a cycle THROUGH A DEV-DEPENDENCY, which Cargo permits and the
# build graph never contains.
liminal-server = { version = "0.9.0", path = "crates/liminal-server" }

anyhow = "1"
async-trait = "0.1"
beamr = { version = "0.19.2", features = ["readiness"] }
blake3 = "1"
chrono = "0.4.45"
clap = "4"
# `default-features = false` bases: members add `std`/`alloc` as they need. The
# `liminal-sdk` crate is `no_std`-capable, so it inherits the lean base and adds
# only `alloc`; the std crates add `std` explicitly. (Cargo forbids a member from
# re-enabling default features it disabled, so the lean base is the only base that
# can satisfy both.)
futures-core = { version = "0.3", default-features = false }
# EXACT PIN, moved =0.9.0 → =0.9.1 in ONE MOTION with the beamr 0.19.1 → 0.19.2
# hop above (2026-08-19; haematite 0.9.1 declares beamr ^0.19.2 on both manifest
# edges — its whole delta vs 0.9.0 is that floor plus cfg(test)-only tests,
# verified at the bytes by Apollo, relayed via Waffles). Pinned BY PUBLISHED
# ARTIFACT, checksums read at the sparse index at fire time:
#   haematite 0.9.1 = a6a994659a8b8b1111489d33f6a19ad59737a0eda53f0b71cfb4b43e27458270
#   beamr     0.19.2 = 0d7223be50040129327f4dec44029b7a426b857cf1837ffb56763a3c2a40f304
# ONE BEAMR PER TREE IS STRUCTURAL, not a tidiness preference: haematite's
# public API carries beamr types across the boundary, so two beamr copies in one
# tree are two incompatible `beamr::` types with identical names — the #9
# duplicate-copy class, which surfaces as unresolvable trait/type mismatches
# rather than as a clean version error. That is why neither pin may be widened
# or bumped alone, in either direction.
# THE NEXT MOVE AGAIN RIDES A HAEMATITE RELEASE ADMITTING IT: when beamr moves
# past ^0.19, this pin does not follow until a haematite whose manifest declares
# that beamr range publishes, and then both lines move in a single commit. The
# target version is re-measured at the index at that trigger, never
# transliterated from a plan's wording.
haematite = "=0.9.1"
js-sys = "0.3.102"
jsonschema = { version = "0.49.9", default-features = false }
rand = "0.10.1"
serde = { version = "1", default-features = false, features = ["derive"] }
serde_json = { version = "1", default-features = false }
signal-hook = "0.4"
socket2 = "0.6"
spin = { version = "0.12", default-features = false, features = ["mutex", "spin_mutex"] }
tempfile = "3"
thiserror = { version = "2", default-features = false }
tokio = "1"
toml = "1.1"
trybuild = "1"
tungstenite = { version = "0.30.0", default-features = false, features = ["handshake"] }
tracing = "0.1"
# The subscriber side of `tracing`. Version-only here (single source of truth);
# the one consumer, `liminal-server`, layers the `env-filter` feature it needs.
tracing-subscriber = "0.3"
uuid = { version = "1.23.3", features = ["v4"] }
wasm-bindgen = "0.2.105"
# Feature-additive: members select the exact Web APIs they bind (web-sys
# compiles only the requested interfaces).
web-sys = "0.3.102"

[workspace.lints.rust]
unsafe_code = "deny"

[workspace.lints.clippy]
all = { level = "deny", priority = -1 }
pedantic = { level = "deny", priority = -1 }
nursery = { level = "warn", priority = -1 }
unwrap_used = "deny"
expect_used = "deny"
panic = "deny"
# Deliberate, project-wide policy decision — NOT a defect suppression.
# Public types are defined in submodules and surfaced both module-qualified
# (liminal::channel::ChannelHandle, liminal::durability::DurableStore) and via
# flat re-exports. The descriptive, greppable names (ChannelHandle, LiminalError)
# are the intended API; the de-prefixed forms (Handle, Error) are generic, less
# searchable, and collide at the crate root. This lint is one mature Rust
# codebases routinely disable for exactly this reason. Centralized here so the
# decision is documented and auditable in one place rather than scattered across
# per-file #![allow] headers.
module_name_repetitions = { level = "allow", priority = 1 }
